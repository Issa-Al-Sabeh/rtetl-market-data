pipeline:
  source:
    type: kafka
    properties:
      bootstrap.servers: localhost:9092
      topic: market-data
      group.id: market-data-etl

  transformations:
    - validate
    - normalize
    - enrich

  sinks:
    - type: kafka
      properties:
        bootstrap.servers: localhost:9092
        topic: market-data-processed