pipeline:
  source:
    type: file
    properties:
      path: data/market-events.jsonl

  transformations:
    - validate
    - normalize
    - enrich

  sinks:
    - type: console
      properties: {}

  deadLetterQueue:
    type: kafka
    properties:
      bootstrap.servers: localhost:9092
      topic: market-data-dlq